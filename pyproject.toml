[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "aws-profile-manager"
version = "0.0.2"
dependencies = [
    "boto3",
    "click",
    "tabulate",
]

[tool.setuptools.packages]
find = {}

[project.scripts]
aws-profile-manager = "src.cli:main"
